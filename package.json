{
  "name": "xeno",
  "version": "1.0.0",
  "description": "la-la-la",
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "build:client": "cd client && npm run build",
    "dev:client": "cd client && npm run dev",
    "vercel:build" : "cd client && npm run build",
    "vercel:start": "cd client && npm run start && cd ../server && npm run start",
    "vercel:install":"cd client && npm install && cd ../server && npm install"
  },
  "author": "ayu",
  "devDependencies": {
  },
  "dependencies": {
  }
}
